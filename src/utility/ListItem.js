export class ListItem {
  constructor(type, title, platform, genres, notes) {
    this.type = type;
    this.title = title;
    this.platform = platform;
    this.genres = genres;
    this.notes = notes;
  }
}